## Phase 3: 구현

TODO.md의 작업을 순차적으로 구현합니다.

### 담당
**Main Agent (Claude 4.5)** 주도
- Claude: 코드 생성, 에러 수정, 리팩토링
- Self-Healing: 검증 실패 시 자동 수정 시도

### 컨텍스트
{context}

### 현재 작업
{current_task}

### 관련 파일
{related_files}

### 구현 원칙
1. **RULES.md 준수**: 코딩 규칙을 엄격히 따릅니다.
2. **타입 힌트**: 모든 함수에 타입 힌트를 사용합니다.
3. **에러 처리**: 적절한 예외 처리를 포함합니다.
4. **문서화**: 필요한 경우 독스트링으로 설명합니다.
5. **테스트 가능**: 테스트하기 쉬운 구조로 작성합니다.

### Self-Healing 프로세스
코드 생성 후 자동으로 검증됩니다:
1. **문법 검사**: AST 파싱 (Python), tsc (TypeScript) 등
2. **타입 검사**: mypy, tsc --strict
3. **린트 검사**: ruff, eslint (자동 수정 적용)

검증 실패 시:
1. 에러 메시지 분석
2. 수정안 도출
3. 재검증 (최대 2회)
4. 실패 시 수동 수정 요청

### 코드 품질 체크리스트
- [ ] 함수당 하나의 책임
- [ ] 중복 코드 제거
- [ ] 매직 넘버 상수화
- [ ] 적절한 에러 메시지
- [ ] 로깅 포함 (필요시)

### 출력 형식

#### 새 파일 생성
```python:src/app/core/config.py
"""설정 모듈."""

from __future__ import annotations

from pydantic import BaseModel, Field


class Config(BaseModel):
    """애플리케이션 설정."""

    app_name: str = Field(default="app")
    debug: bool = Field(default=False)
```

#### 기존 파일 수정
```diff:src/app/core/config.py
 class Config(BaseModel):
     app_name: str = Field(default="app")
     debug: bool = Field(default=False)
+    log_level: str = Field(default="INFO")
```

### 변경사항 요약
- **무엇을**: 변경한 내용
- **왜**: 변경 이유
- **주의**: 사용 시 주의사항

### 외부 정보 필요 시
```
[NEED_KNOWLEDGE: 라이브러리 X의 최신 API 사용법]
```

Gemini에게 질의하여 정보를 얻습니다.
