# Project Analysis & Improvement Roadmap: Vibe Coding Helper

## 1. 개요 (Overview)

본 문서는 `vibe-coding-helper` 프로젝트의 현 상태를 분석하고, 시스템의 안정성, 확장성 및 사용자 경험(UX)을 향상시키기 위한 기술적 보완점을 제안합니다.

**최종 업데이트**: 2026-02-04
**현재 진행률**: 86% (49/57 작업 완료)

---

## 2. 현 상태 분석 (Current Analysis)

### 2.1 아키텍처 강점

| 구성 요소 | 상태 | 설명 |
|----------|------|------|
| **Phase-Based Workflow** | ✅ 완료 | Phase 0~4까지 단계별 문서 생성 및 진행 |
| **Dual Model Strategy** | ✅ 완료 | Claude(Main Agent) + Gemini(Knowledge Engine) 협업 |
| **Self-Healing** | ✅ 완료 | 코드 생성 후 자동 검증 및 수정 |
| **Multi-Language Verifier** | ✅ 완료 | Python, TypeScript, JavaScript, Java, Flutter/Dart 지원 |
| **CLI Commands** | ✅ 완료 | init, plan, design, scaffold, code, test, verify, status, chat, undo |

### 2.2 기술 스택 적합성

- **Typer & Rich**: CLI 환경에서 강력한 UI/UX 제공
- **Pydantic**: 모든 데이터 모델에 대한 엄격한 검증으로 런타임 오류 방지
- **asyncio**: 비동기 AI API 호출로 성능 최적화

### 2.3 구현 완료 모듈

```
src/vibe/
├── cli/commands/          # 10개 명령어 모두 구현
│   ├── init.py           ✅ Gemini 단독 - 프로젝트 초기화
│   ├── plan.py           ✅ Gemini+Claude - PRD/User Stories
│   ├── design.py         ✅ Claude+Gemini - TREE/SCHEMA
│   ├── scaffold.py       ✅ 디렉토리/파일 생성
│   ├── code.py           ✅ Claude+Self-Healing - 코드 구현
│   ├── test.py           ✅ Gemini(감사)+Claude(엣지케이스)
│   ├── verify.py         ✅ 다국어 코드 검증
│   ├── status.py         ✅ 진행 상황 표시
│   ├── chat.py           ✅ 자유 대화 모드
│   └── undo.py           ✅ Git 롤백
├── providers/
│   └── orchestrator.py   ✅ 듀얼 모델 오케스트레이션
├── verifiers/            ✅ 언어별 검증기 시스템
│   ├── python.py         ✅ ast, mypy, ruff, pytest
│   ├── typescript.py     ✅ tsc, eslint, vitest/jest
│   ├── java.py           ✅ javac, checkstyle, maven/gradle
│   └── flutter.py        ✅ dart analyze, flutter test
└── core/
    ├── context.py        ✅ Dual-Track Context (Hot/Cold)
    └── config.py         ✅ DualModelConfig 포함
```

---

## 3. 아키텍처 다이어그램

### 3.1 Phase별 모델 역할 분담

```
┌─────────────────────────────────────────────────────────────────┐
│                    Vibe Coding Helper                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Phase 0 (Init)     ──▶  [Gemini 단독]                          │
│                          사용자 의도 파악, 기술 스택 제안         │
│                                                                  │
│  Phase 1 (Plan)     ──▶  [Gemini] ──▶ [Claude]                  │
│                          컨텍스트 분석    PRD 작성               │
│                                                                  │
│  Phase 2 (Design)   ──▶  [Claude] ──▶ [Gemini]                  │
│                          아키텍처 설계    라이브러리 검증         │
│                                                                  │
│  Phase 3 (Code)     ──▶  [Claude] ◀──▶ [Gemini]                 │
│                          자율 코딩        컨텍스트 주입           │
│                              │                                   │
│                              ▼                                   │
│                         [Verifier]                               │
│                          자동 검증                               │
│                              │                                   │
│                         실패 시 ▼                                │
│                        [Self-Healing]                            │
│                          자동 수정                               │
│                                                                  │
│  Phase 4 (Test)     ──▶  [Gemini] + [Claude]                    │
│                          전역 감사    엣지 케이스 생성            │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 검증기 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                      LanguageVerifier (ABC)                      │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐      │
│  │check_syntax │ check_types │ check_lint  │ run_tests   │      │
│  └─────────────┴─────────────┴─────────────┴─────────────┘      │
└─────────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ PythonVerifier  │ │TypeScriptVerifier│ │  JavaVerifier   │
│                 │ │                 │ │                 │
│ • ast.parse()   │ │ • tsc --noEmit  │ │ • javac         │
│ • mypy          │ │ • eslint        │ │ • checkstyle    │
│ • ruff          │ │ • vitest/jest   │ │ • maven/gradle  │
│ • pytest        │ │                 │ │                 │
└─────────────────┘ └─────────────────┘ └─────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│JavaScriptVerifier│ │  DartVerifier   │ │ FlutterVerifier │
│                 │ │                 │ │                 │
│ • node --check  │ │ • dart analyze  │ │ • flutter analyze│
│ • eslint        │ │ • dart test     │ │ • flutter test  │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### 3.3 Self-Healing 워크플로우

```
┌──────────────┐
│  코드 생성   │
│   (Claude)   │
└──────┬───────┘
       │
       ▼
┌──────────────┐     성공      ┌──────────────┐
│  문법 검증   │ ───────────▶  │  린트 자동   │
│  (Verifier)  │               │    수정      │
└──────┬───────┘               └──────────────┘
       │ 실패
       ▼
┌──────────────┐
│ Self-Healing │
│   (Claude)   │◀────┐
└──────┬───────┘     │
       │             │ 재시도
       ▼             │ (최대 2회)
┌──────────────┐     │
│   재검증     │─────┘
└──────┬───────┘
       │ 실패
       ▼
┌──────────────┐
│  수동 수정   │
│    필요      │
└──────────────┘
```

---

## 4. 보완 제안 및 진행 상황

### 4.1 완료된 개선 사항 ✅

| 항목 | 상태 | 구현 내용 |
|------|------|----------|
| 다국어 검증기 시스템 | ✅ 완료 | `src/vibe/verifiers/` - 5개 언어 지원 |
| Self-Healing 통합 | ✅ 완료 | `code.py`에 자동 검증 및 수정 로직 |
| 듀얼 모델 오케스트레이터 | ✅ 완료 | `orchestrator.py` - Phase별 역할 분담 |
| Dual-Track Context | ✅ 완료 | Hot Memory (Claude) / Cold Storage (Gemini) |
| CLI 명령어 전체 구현 | ✅ 완료 | 10개 명령어 모두 작동 |
| `vibe verify` 명령어 | ✅ 완료 | 독립적 코드 검증 CLI |

### 4.2 진행 중인 개선 사항 🔄

| 우선순위 | 작업 항목 | 상태 | 기대 효과 |
|:--------:|----------|:----:|----------|
| **High** | `Generator` 클래스 분리 | 🔄 제안됨 | 유지보수성 향상 |
| **High** | `MarkdownCodeParser` 개선 | 🔄 제안됨 | AI 응답 파싱 성공률 향상 |
| **Medium** | 로깅 시스템 도입 | 🔄 제안됨 | 디버깅 효율성 향상 |
| **Medium** | Self-Healing 모듈화 | 🔄 제안됨 | 다른 Phase에서도 활용 |

### 4.3 미완료 작업 📋

| 작업 ID | 설명 | 우선순위 |
|---------|------|:--------:|
| DUAL-003 | Self-Healing 워크플로우 모듈화 | Should |
| CODE-029 | 시스템 프롬프트 업데이트 | Must |
| CODE-030 | Phase별 프롬프트 업데이트 | Must |
| CODE-032 | 문서 템플릿 (Jinja2) | Should |
| TEST-001~005 | 단위/통합 테스트 | Should |
| DOC-001~002 | README 및 사용법 문서화 | Must |

---

## 5. 확장 가이드

### 5.1 새 언어 검증기 추가

```python
# src/vibe/verifiers/go.py
from vibe.verifiers.base import LanguageVerifier, VerifyResult, VerifyLevel

class GoVerifier(LanguageVerifier):
    language = "Go"
    extensions = [".go"]

    def check_syntax(self, file_path: Path) -> VerifyResult:
        # go build로 문법 검사
        result = subprocess.run(
            ["go", "build", "-o", "/dev/null", str(file_path)],
            capture_output=True, text=True
        )
        return VerifyResult(
            success=result.returncode == 0,
            check_type=VerifyLevel.SYNTAX,
            output=result.stderr
        )

    def check_types(self, file_path: Path) -> VerifyResult:
        # Go는 컴파일 시 타입 체크
        return VerifyResult(success=True, check_type=VerifyLevel.TYPES)

    def check_lint(self, file_path: Path, fix: bool = False) -> VerifyResult:
        # golangci-lint 사용
        cmd = ["golangci-lint", "run"]
        if fix:
            cmd.append("--fix")
        # ...

    def run_tests(self, file_path: Path) -> VerifyResult:
        # go test 사용
        # ...
```

```python
# 등록
from vibe.verifiers.factory import register_verifier
register_verifier(".go", GoVerifier)
```

### 5.2 새 AI Provider 추가

```python
# src/vibe/providers/custom.py
from vibe.providers.base import AIProvider, Response

class CustomProvider(AIProvider):
    name = "custom"

    async def generate(self, messages, system=None, max_tokens=4096) -> Response:
        # API 호출 로직
        ...

    async def stream(self, messages, system=None):
        # 스트리밍 로직
        ...
```

### 5.3 새 Phase 추가

1. `src/vibe/prompts/phases/phase5_deploy.txt` 생성
2. `orchestrator.py`에 `execute_phase5_deploy()` 메서드 추가
3. `workflow.py`의 `PHASE_NAMES`에 Phase 5 추가
4. `state.py`의 phase 범위 수정 (`le=5`)
5. 새 CLI 명령어 `deploy.py` 생성

---

## 6. 제안: Generator 패턴 리팩토링

```python
# src/vibe/core/generator.py (제안)
from dataclasses import dataclass
from pathlib import Path

@dataclass
class GenerationResult:
    success: bool
    file_path: Path
    content: str
    verification_passed: bool
    self_healing_applied: bool
    issues: list[str]

class CodeGenerator:
    """코드 생성, 검증, Self-Healing을 캡슐화."""

    def __init__(self, config: VibeConfig):
        self.config = config
        self.orchestrator = create_orchestrator(config.dual_mode)

    async def generate_and_verify(
        self,
        task: Task,
        context: DualTrackContext,
        max_healing_attempts: int = 2
    ) -> GenerationResult:
        """
        1. AI로 코드 생성
        2. 검증기로 검증
        3. 실패 시 Self-Healing 시도
        4. 최종 결과 반환
        """
        # 1. 코드 생성
        code = await self._generate_code(task, context)

        # 2. 검증
        verify_result = self._verify_code(code, task.file_path)

        # 3. Self-Healing
        healing_applied = False
        for attempt in range(max_healing_attempts):
            if verify_result.success:
                break
            code = await self._self_heal(code, verify_result, context)
            verify_result = self._verify_code(code, task.file_path)
            healing_applied = True

        return GenerationResult(
            success=verify_result.success,
            file_path=task.file_path,
            content=code,
            verification_passed=verify_result.success,
            self_healing_applied=healing_applied,
            issues=[str(i) for i in verify_result.issues]
        )
```

---

## 7. 성능 메트릭 (예상)

| 지표 | 현재 | 목표 |
|------|:----:|:----:|
| 코드 생성 성공률 (1차 시도) | ~70% | 85% |
| Self-Healing 후 성공률 | ~90% | 95% |
| 평균 생성 시간 (파일당) | ~15초 | ~10초 |
| 지원 언어 수 | 5개 | 8개+ |

---

## 8. 로드맵

```
2026 Q1                    2026 Q2                    2026 Q3
    │                          │                          │
    ▼                          ▼                          ▼
┌─────────────┐          ┌─────────────┐          ┌─────────────┐
│ v1.0 Release│          │ v1.1 Release│          │ v2.0 Release│
│             │          │             │          │             │
│ • 현재 기능 │          │ • Go, Rust  │          │ • Web UI    │
│   안정화    │          │   검증기    │          │ • 팀 협업   │
│ • 테스트    │          │ • Generator │          │ • 클라우드  │
│   커버리지  │          │   패턴 적용 │          │   연동      │
│ • 문서화    │          │ • 로깅 시스템│          │             │
└─────────────┘          └─────────────┘          └─────────────┘
```

---

## 9. 결론

Vibe Coding Helper는 **86%의 핵심 기능**이 구현되어 있으며, 특히:

1. **듀얼 모델 전략**이 효과적으로 작동
2. **5개 언어 검증기**가 통합되어 다양한 프로젝트 지원
3. **Self-Healing**으로 AI 생성 코드의 품질 보장

남은 작업은 주로 **테스트, 문서화, 리팩토링**이며, 핵심 기능은 프로덕션 준비 단계에 있습니다.

---

*최종 업데이트: 2026-02-04*
*분석 도구: Claude + Gemini Dual Analysis*
